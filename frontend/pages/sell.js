import CreateItem from '../components/CreateItem';
import PleaseSign from '../components/PleaseSign';

const Sell = props => (
  <div>
    <PleaseSign>
      <CreateItem />
    </PleaseSign>
  </div>
);

export default Sell;
